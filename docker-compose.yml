services:
  postgres:
    image: postgres:17-alpine
    container_name: capitalflow-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-capitalflow}
      POSTGRES_USER: ${POSTGRES_USER:-capitalflow}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-capitalflow}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
